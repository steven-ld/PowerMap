# Rust build output
/target
**/*.rs.bk
*.pdb

# Runtime data / credentials — never commit these
/data
*.key
*.credential.json
powermap-server.toml
powermap-client.toml
# Unified configuration and identity names used by the single-binary migration.
powermap.toml
powermap.key

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
*.swp
*~

# Local AI assistant settings and generated workspace metadata
/.agents/
/.claude/
